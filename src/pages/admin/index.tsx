import { COLOR } from "@/styles/color";

export default function SignIn() {
  return (
    <div style={{
      margin: '24px 16px',
      padding: 24,
      minHeight: '200',
      background: COLOR.defaultBackground,
      borderRadius: 8,
    }}>
      Admin Dashboard
    </div>
  );
}
